# B.SORI 기능 시연 가이드

## 한 문장 소개

B.SORI는 부산에서 발생하는 수산 부산물을 버려지는 폐기물이 아니라 바이오가스 원료로 연결하고, 등록부터 수거·운송·자원화·에너지 활용까지 한 화면에서 관리하는 통합 플랫폼입니다.

## 시연 전 준비

1. 통합관리자 계정으로 로그인합니다.
2. 화면 상단의 `시연 데이터 켜기`를 누릅니다.
3. 상단 표시가 `LIVE`에서 `DEMO`로 바뀌었는지 확인합니다.
4. `계정·연동 설정`에서 배출업체, 수거기사, 자원화시설, 통합관리자 화면을 전환합니다.

시연 데이터는 실제 Supabase 운영 데이터에 저장되지 않으므로 자유롭게 버튼을 눌러도 됩니다.

## 5분 시연 순서

### 1. 통합 현황 — 40초

“먼저 관리자는 부산 전체의 부산물 발생량과 현재 처리 단계를 한눈에 봅니다. 등록, 수거, 자원화, 에너지 전환까지 하나의 공급망으로 연결되어 있습니다.”

- 등록 물량과 진행 건수 확인
- 공급망 단계별 건수 확인
- 부산 기상 위험도 확인

### 2. 배출업체 — 50초

`계정·연동 설정 → 배출업체 → 화면 이동`을 선택합니다.

“배출업체는 부산물 종류, 예상 수량, 수거 장소와 사진만 입력하면 됩니다. 사진은 Gemini가 종류와 상태를 분석하고, 등록된 요청은 관리자에게 전달됩니다.”

- 이미 채워진 해원수산 예시 정보 확인
- `수거 요청 등록 시연` 클릭
- 최근 요청과 처리 상태 확인

### 3. 관리자 배차 — 50초

통합관리자 화면으로 돌아가 `수거 운영`을 선택합니다.

“관리자는 접수된 요청에 기사와 차량을 배정합니다. 위치가 입력된 수거지는 가까운 순서로 정렬되고 카카오맵에서 실제 도로 경로와 예상 시간을 계산합니다.”

- 해원수산 요청 선택
- 김도윤 기사와 부산 82가 2481 차량 확인
- `배차 확정 시연` 클릭
- 지도와 방문 순서 확인

### 4. 수거기사 — 50초

`계정·연동 설정 → 수거기사 → 화면 이동`을 선택합니다.

“기사는 모바일 화면에서 오늘의 방문 순서, 주소, 예상 수거량과 날씨 위험도를 확인합니다. 운행 시작, 도착, 수거 완료 순서로 상태를 기록할 수 있습니다.”

- 첫 번째 요청의 상태 버튼을 차례로 클릭
- 지도와 수거 일정 확인

### 5. 자원화시설 — 50초

`계정·연동 설정 → 자원화시설 관리자 → 화면 이동`을 선택합니다.

“시설에서는 도착 차량을 선택하고 실계량 중량, 품질, 이물질, 처리 방식을 기록합니다. 이 정보가 바이오가스 원료 확보량과 처리 실적으로 이어집니다.”

- 입고 예정 차량 선택
- 계량·품질·처리 방식 확인
- `검수 완료 및 반입 처리` 클릭

### 6. AI 예측과 에너지 — 60초

통합관리자 화면으로 돌아가 `AI 예측`, `자원화·에너지`를 차례로 선택합니다.

“누적된 등록 이력은 다음 부산물 발생량과 필요한 차량 수를 예측하는 데 사용됩니다. 수거된 부산물은 혼합소화 원료로 계산되어 예상 바이오가스와 발전량으로 연결됩니다.”

- 7일 부산물 발생량 예측 확인
- 수거 우선순위 확인
- 확보 원료, 바이오가스, 발전량 추정 확인

ESS와 시설 PLC는 실제 설비 API가 제공되면 실시간 제어값으로 전환되는 항목이며, 현재는 미연동 상태를 명확히 표시합니다.

## 마무리 멘트

“B.SORI의 핵심은 역할별로 흩어진 업무를 하나의 데이터 흐름으로 만드는 것입니다. 배출업체의 등록이 기사 배차와 시설 처리로 즉시 이어지고, 누적 데이터는 부산물 발생 예측과 바이오가스 공급 계획에 다시 활용됩니다. 이를 통해 불법 투기와 처리 지연을 줄이고, 수산 부산물을 부산의 지역 에너지 자원으로 전환할 수 있습니다.”

## 질문을 받았을 때 강조할 점

- 실제 데이터는 Supabase와 권한 정책으로 계정별 접근 범위를 제한합니다.
- 사진 분석은 Gemini, 주소와 이동 경로는 Kakao API를 사용합니다.
- 기사 화면은 모바일과 PWA 사용을 고려했습니다.
- 부산 위판 원천 데이터, 시설 PLC, ESS BMS는 제공 기관의 API 규격을 받으면 실제 실시간 데이터로 연결할 수 있습니다.
- 시연 모드는 실제 운영 데이터를 변경하지 않습니다.
